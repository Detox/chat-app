dom-module(id="detox-chat-app-sidebar-settings")
	link(rel="import" href="style.css" type="css")
	template
		csw-form: form
			label
				div Announce to the network*:
				csw-icon(mono icon="question" on-click="_help_settings_announce")
			csw-group
				csw-label-button(value="{{settings_announce}}"): label
					input(type="radio" value="0")
					| No
				csw-label-button: label
					input(type="radio" value="1")
					| Yes
			label
				div Bootstrap nodes*:
				csw-icon(mono icon="question" on-click="_help_settings_bootstrap_nodes")
			csw-textarea: textarea(rows="10" wrap="off" value="{{bootstrap_nodes_string}}" on-blur="_bootstrap_node_blur")
		p.restart-warning * Changing these settings require application restart in order to take effect
	script(src="script.js")
